{
  "name": "treema-todo-assignment",
  "private": true,
  "description": "Minimalist ToDo client-server application built with Next and Node.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build -w @treema-todo-assignment/shared && npm run build -w @treema-todo-assignment/server && npm run build -w @treema-todo-assignment/client",
    "dev:server": "npm run dev -w @treema-todo-assignment/server",
    "dev:client": "npm run dev -w @treema-todo-assignment/client",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"magenta,cyan\" \"npm run dev:server\" \"npm run dev:client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "concurrently": "^9.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
